// This is a function that times how long functions takes to run in C
#include <time.h>
#include <stdlib>

// Make this function not need the exact number of parameters for a function
// Need to make use of Vardiac function call
int (void (*function), int, int){
    int time;
    return 0;
}
